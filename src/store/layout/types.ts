export interface LayoutState {
    sidebar: boolean,
    settings: boolean,
    background: boolean
    background_modules: boolean
    search: boolean
    popup: boolean
    profile: boolean
    notifications: boolean
}