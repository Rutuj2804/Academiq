import { CircularProgress } from "@mui/material";
import React from "react";

const Loader = () => {
    return (
        <div className="academiq__Progress">
            <CircularProgress />
        </div>
    );
};

export default Loader;
